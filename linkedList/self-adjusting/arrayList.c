#include "arrayList.h"

int*
makeEmptyList(int N)
{
  return calloc(N, sizeof(int));
}

void insert(int elem, int list[]);
{
  if (list[0] == 0)
  {
    list[0] = elem;
    return;
  }
  else
  {
    

  }
}

int find(int elem, int list[]);
{


}
